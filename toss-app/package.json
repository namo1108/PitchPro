{
  "name": "pitchpro-toss",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node copy-assets.cjs",
    "ait:build": "npm run build && npx ait build",
    "ait:deploy": "npx ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.0.3"
  },
  "devDependencies": {
    "@apps-in-toss/devtools": "^3.0.3"
  }
}
